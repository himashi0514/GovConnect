import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import Button from '../../components/Button';
import Logo from '../../components/Logo';
import { ArrowLeft, Droplets, Search } from 'lucide-react';

const RMVStaffDeliveryStatus = () => {
  const navigate = useNavigate();
  const [searchLicense, setSearchLicense] = useState("");

  const handleGoBack = () => navigate(-1);

  const handleSearchLicense = () => {
    console.log('Searching for license:', searchLicense);
    // Add your search logic here
  };

  const handleUpdate = (licenseNumber) => {
    console.log('Updating status for license:', licenseNumber);
    // Add your update logic here
  };

  // Sample delivery data
  const deliveryData = [
    { licenseNumber: "CAM 5434", status: "Dispatched" },
    { licenseNumber: "CAM 5434", status: "In transit" },
    { licenseNumber: "CAM 5434", status: "Delivered" },
    { licenseNumber: "CAM 5434", status: "Dispatched" },
    { licenseNumber: "CAM 5434", status: "In Transit" },
    { licenseNumber: "CAM 5434", status: "Dispatched" }
  ];

  return (
    <div style={styles.body}>
      <div style={styles.container}>
        <div style={styles.header}>
          <div style={styles.backArrow} onClick={handleGoBack}>
            <ArrowLeft size={24} color="#000000" />
          </div>
          <div style={styles.headerContent}>
            <div style={styles.dropletIcon}>
              <Droplets size={24} color="#47a7ff" />
            </div>
            <h1 style={styles.title}>R.M.V Staff</h1>
          </div>
        </div>
        
        <div style={styles.content}>
          <h2 style={styles.sectionTitle}>Delivery Status</h2>
          
          {/* Search Section */}
          <div style={styles.searchSection}>
            <div style={styles.searchContainer}>
              <Search size={20} color="#999" style={styles.searchIcon} />
              <input
                type="text"
                placeholder="Search License Number"
                value={searchLicense}
                onChange={(e) => setSearchLicense(e.target.value)}
                style={styles.searchInput}
                onKeyPress={(e) => {
                  if (e.key === 'Enter') {
                    handleSearchLicense();
                  }
                }}
              />
            </div>
          </div>

          {/* Table Header */}
          <div style={styles.tableHeader}>
            <div style={styles.headerCell}>Licence Number</div>
            <div style={styles.headerCell}>Delivery Status</div>
          </div>

          {/* Delivery Rows */}
          <div style={styles.deliveryList}>
            {deliveryData.map((item, index) => (
              <div key={index} style={styles.deliveryRow}>
                <div style={styles.licenseCell}>
                  {item.licenseNumber}
                </div>
                <div style={styles.statusCell}>
                  <span style={styles.statusText}>{item.status}</span>
                  <button 
                    style={styles.updateButton}
                    onClick={() => handleUpdate(item.licenseNumber)}
                    onMouseEnter={(e) => {
                      e.target.style.backgroundColor = '#1e40af';
                    }}
                    onMouseLeave={(e) => {
                      e.target.style.backgroundColor = '#3b82f6';
                    }}
                  >
                    Update
                  </button>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

const styles = {
  body: {
    fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif',
    backgroundColor: '#f5f5f5',
    padding: '20px',
    margin: 0,
    minHeight: '100vh'
  },
  container: {
    maxWidth: '400px',
    margin: '0 auto',
    backgroundColor: 'white',
    borderRadius: '16px',
    overflow: 'hidden',
    boxShadow: '0 4px 6px rgba(0, 0, 0, 0.1)',
    minHeight: '80vh'
  },
  header: {
    backgroundColor: 'white',
    padding: '20px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    position: 'relative',
    borderBottom: '1px solid #e5e5e5'
  },
  backArrow: {
    position: 'absolute',
    left: '20px',
    cursor: 'pointer',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  },
  headerContent: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  },
  dropletIcon: {
    marginBottom: '8px'
  },
  title: {
    color: '#000000',
    fontSize: '24px',
    fontWeight: '700',
    textAlign: 'center',
    margin: 0
  },
  content: {
    padding: '20px',
    display: 'flex',
    flexDirection: 'column',
    gap: '20px'
  },
  sectionTitle: {
    color: '#000000',
    fontSize: '18px',
    fontWeight: '600',
    margin: '0 0 0 0'
  },
  searchSection: {
    width: '100%'
  },
  searchContainer: {
    position: 'relative',
    display: 'flex',
    alignItems: 'center'
  },
  searchIcon: {
    position: 'absolute',
    left: '16px',
    zIndex: 1
  },
  searchInput: {
    width: '100%',
    padding: '16px 16px 16px 48px',
    fontSize: '16px',
    border: '1px solid #e5e5e5',
    borderRadius: '8px',
    backgroundColor: '#f8f9fa',
    color: '#666',
    outline: 'none',
    boxSizing: 'border-box'
  },
  tableHeader: {
    display: 'flex',
    backgroundColor: '#033ea1',
    borderRadius: '8px 8px 0 0',
    overflow: 'hidden'
  },
  headerCell: {
    flex: 1,
    padding: '12px 16px',
    color: 'white',
    fontSize: '14px',
    fontWeight: '600',
    textAlign: 'center'
  },
  deliveryList: {
    backgroundColor: '#f8f9fa',
    borderRadius: '0 0 8px 8px',
    border: '1px solid #e5e5e5',
    borderTop: 'none'
  },
  deliveryRow: {
    display: 'flex',
    borderBottom: '1px solid #e5e5e5'
  },
  licenseCell: {
    flex: 1,
    padding: '16px',
    color: '#000000',
    fontSize: '14px',
    fontWeight: '600',
    textAlign: 'left',
    display: 'flex',
    alignItems: 'center'
  },
  statusCell: {
    flex: 1,
    padding: '12px 16px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: '8px'
  },
  statusText: {
    color: '#000000',
    fontSize: '14px',
    fontWeight: '500'
  },
  updateButton: {
    backgroundColor: '#3b82f6',
    color: 'white',
    border: 'none',
    borderRadius: '4px',
    padding: '6px 12px',
    fontSize: '12px',
    fontWeight: '500',
    cursor: 'pointer',
    transition: 'background-color 0.2s ease'
  }
};

export default RMVStaffDeliveryStatus;